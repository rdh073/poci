<?xml version="1.0" encoding="utf-8"?>
<!-- Splash icon: the full-colour Poci teapot mascot, centered on the theme-following splash background.
     It is multi-colour (navy body / amber lid / cream face), so it is NOT tinted — the splash background
     (@color/splash_background) still adapts light/dark via values / values-night. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/splash_robot"
    android:gravity="center" />
